GitHub Actions auto-deploy secrets for Jamly:

- JAMLY_HOST
- JAMLY_PORT
- JAMLY_USERNAME
- JAMLY_PASSWORD

After these are set in GitHub repository secrets, every push to `main` will auto-deploy to the server.
